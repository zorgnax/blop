`int main (char **argv, int argc);`

an ampersand: `&`.

the form tag: `<form></form>`.

whitespace around: `` foo ``.

`` inner ` backtick ``

many inner ```````` `````` ```````` backticks.

from `<!--` to `-->`
on two lines.

from `<!--`
to `-->`
on three lines.
