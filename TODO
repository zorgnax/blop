category listing pages need to show the category in a heading on the page
the 1 - 14 of 16 posts needs its own line, not floated right, possibly combined with category heading.
sidebar looks wrong
settings updated page

