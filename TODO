give backups and wordpress migrations their own plugin, maybe
clean migratewp
write your own templating system
posts and pages lists in admin should have a date mouseover
remove p from tar xvpf so if we cache templates, updates will be seen in cached template compiles

